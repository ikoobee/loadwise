{
  "name": "loadwise",
  "version": "0.1.0",
  "private": true,
  "description": "Container loading planner for cross-border shipping — constraint-aware 3D packing engine (deterministic, weight & stack limits, step-by-step loading guide).",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p packages/core"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
